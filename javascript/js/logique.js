var A = true, B = false;
/**
 * l' operateur logique and(et): &&
 * verifie si 2 conditions ou plus sont vrai ou fausse 
 */
if ((prenomLogin === prenom) && (age === monage));{
    var prenom = "lazo";
    alert((prenomLogin === prenom) && (age === monage));

    else{
        aler("prenom ou age invalide");
    }
      }

if ((A) && (B))
    // => si A est faux b est vrai => retourne faux
     // => si A est vrai b est faux => retourne faux
      // => si A est faux b est faux => retourne faux
       // => si A est vrai b est vrai => retourne vrai


/**
 * l' operateur logique or(et): //
 */
if ((A) && (B))
// => si A est faux b ou vrai => retourne faux
     // => si A est vrai b ou faux => retourne vrai
      // => si A est faux b ou faux => retourne vrai
       // => si A est vrai b ou vrai => retourne vrai

/**
 * l' operateur logique not(non): //
 *cette operateur rend vrai ce qui est faux et inversement 
 */


var user = true;
if(!user){
    // code si l' utilisateur n' est pas loge
}
// ce qui revient a ecrire:
if (user == false){
    if(user){
        if(user == true){}
    }
}



